# Copy to /etc/sompi/cloudflared.env, set mode 0600, and replace the token
# with the token for the named Cloudflare Tunnel that routes your public
# hostname to http://localhost:8642.
CLOUDFLARED_TOKEN=REPLACE_ME
